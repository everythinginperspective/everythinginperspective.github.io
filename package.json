{
  "name": "everythinginperspective",
  "version": "1.0.0",
  "description": "A magazine exploring trends in perspective",
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "generate": "nuxt generate && rm -rf docs && cp -r .output/public docs && pagefind --site docs",
    "dev": "nuxt dev",
    "start": "nuxt preview",
    "deploy": "npm run generate && git add docs && git commit -m 'Deploy to GitHub Pages' && git push origin main"
  },
  "keywords": [],
  "author": "Everything in Perspective",
  "license": "MIT",
  "dependencies": {
    "@nuxt/content": "*",
    "@nuxtjs/i18n": "*",
    "@nuxtjs/seo": "*",
    "@nuxtjs/sitemap": "*",
    "better-sqlite3": "*",
    "nuxt": "4.4.4",
    "nuxt-og-image": "*",
    "nuxt-schema-org": "*",
    "nuxt-seo-utils": "*",
    "nuxt-skew-protection": "*",
    "pagefind": "*",
    "pluralize": "*",
    "vue": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "vue-tsc": "*"
  }
}
