name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write
  deployments: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.21.1'
          cache: 'npm'

      - name: Check npm and node version (before)
        run: |
          echo "Node version:"
          node --version
          echo "npm version:"
          npm --version

      - name: Install npm 10.9.4
        run: npm install -g npm@10.9.4

      - name: Check npm and node version (after)
        run: |
          echo "Node version:"
          node --version
          echo "npm version:"
          npm --version

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Generate static site
        run: NODE_OPTIONS="--max-old-space-size=4096" npm run generate

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.output/public'

  deploy:
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
